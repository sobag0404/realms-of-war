# Local alpha defaults. Do not commit production secrets.
DATABASE_URL="file:./dev.db"
NEXT_TELEMETRY_DISABLED=1
# Production disables server-side save APIs unless this local-alpha opt-in is set.
# REALMS_SERVER_SAVES=local-alpha
