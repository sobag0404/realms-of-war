{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default desktop capability for the main Realms of War window.",
  "windows": ["main"],
  "permissions": ["core:default"]
}

